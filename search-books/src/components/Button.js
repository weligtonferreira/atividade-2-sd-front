import styled from 'styled-components';

export const Button = styled.button`
  border: none;
  border-radius: 0.6rem;
  background-color: var(--submit-buton);

  &:hover {
    cursor: pointer;
  }
`;
